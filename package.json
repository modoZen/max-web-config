{
  "name": "max-web-config",
  "version": "0.0.0",
  "description": "Library to config eslint, prittier, lint-staged and jest",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max Diaz"
}
